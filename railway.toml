[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --worker-class gthread --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:${PORT:-8080} websocket_api:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
